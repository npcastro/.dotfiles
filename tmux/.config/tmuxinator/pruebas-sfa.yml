# /Users/nicolascastro/.config/tmuxinator/pruebas-sfa.yml

name: pruebas-sfa
root: ~/workspace/fintoc/pruebas-sfa

windows:
  - editor:
      panes:
        - nvim
  - server:
  - terminal:
  - lazygit:
      panes:
        - lazygit
